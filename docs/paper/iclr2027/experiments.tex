% v10 (2026-09-06, sec5 reorg -- user calls, background session).
%   Main-text results = DINOv2 table + runtime table + stage ablation.
%     - Datasets: miniImageNet REMOVED from the comparison entirely
%       (roster = CIFAR-10 / CIFAR-100 / EuroSAT); vinyals2016matching
%       cite dropped from 5.1.
%     - Budget grid: tables stop at 12 shots (columns {2,4,8,12}); the
%       s=14 runs stay in output/headline for the record.
%     - CLIP table MOVED to Appendix app:backbones and UPGRADED to CLIP
%       ViT-L (d=1024): the large encoder STRENGTHENS the claim (FRCP
%       smallest 7/9 a01 + 8/9 a005 vs 6/9 + 6/9 on ViT-B; FRCP sets
%       ~halve on cifar100, 21.1->11.7 at s=2). clip-l grid currently
%       {2,4,8}: no s=12 CLIP run exists yet.
%     - Runtime NEW main-text table (tab:runtime-cifar100):
%       headline_timing_benchmark.py rerun at shots {2,4,8,12} +
%       make_runtime_table.py (medians over 5 reps). CV+ 0.34->3.75s vs
%       FRCP 0.78->1.32s. Old runtime paragraph replaced.
%     - Figure 3 -> fig_stage_ablation_row.pdf (1x3, three datasets);
%       the v9 "all eight subsets" prose FIXED to the six combos the
%       09-01 run actually holds (raw/T/W/S/TW/full; the 8-subset run
%       lives on the unmerged exp/temperature-audit branch).
%     - Score-hist figure MOVED to Appendix app:scorehist and
%       REGENERATED on the deployed prototype-softmax score with a
%       logit-scaled axis (plot_refine_score_hist.py --score softmax
%       --xscale logit); main text keeps the paragraph as a pointer with
%       the new softmax leak numbers (0.55->0.02 / 1.50->0.42 /
%       1.84->1.56).
%     - 5.2 prose recomputed for the 3x4 grid: FRCP smallest 11/12
%       (single loss eurosat s12 to CV+, 1.31 vs 1.44), cov 0.899-0.945,
%       SemiCP below target in 3 cells, min 0.884.
%     - The v9 "implementation(adaptations" missing space is FIXED here
%       (was carried-as-pasted from Overleaf).
%     - OPEN: the non-empty-sets variant (other session) may replace the
%       results JSONs; tables regenerate with one command
%       (make_headline_tables.py --headline_dir <new_dir>). OPEN: CLIP
%       s=12 cells need a run if the appendix grid should reach 12.
% ----------------------------------------------------------------------
% v9 history (2026-09-02/03) kept for provenance: two headline tables
%   (ours last), stage-ablation figure, cosine score-hist figure,
%   09-03 alignment to the Overleaf gold. See git history of this file.
% Float conventions (ICLR): table caption ABOVE the table, figure
%   caption BELOW the figure, both centered; booktabs.
% OVERLEAF: preamble needs \usepackage{graphicx}, \usepackage{booktabs},
%   \usepackage{multirow}; upload figs/fig_stage_ablation_row.pdf and
%   (appendix) figs/fig_refine_score_hist_softmax.pdf.

\section{Experiments}
\label{sec:experiments}
\subsection{Setup}
\label{sec:setup}
\textbf{Datasets.} We evaluate on three standard image-classification
benchmarks. CIFAR-10 and CIFAR-100~\citep{krizhevsky2009learning} are
consumer photographs of the kind the backbone saw in pre-training,
while EuroSAT~\citep{helber2019eurosat} is Sentinel-2 satellite imagery
and sits further from it. Embeddings come from the frozen DINOv2
ViT-B/14 encoder~\citep{oquab2023dinov2}, of dimension $d = 768$, and
Appendix~\ref{app:backbones} repeats every comparison on CLIP
ViT-L~\citep{radford2021clip}, of dimension $d = 1024$. For each
dataset the unlabeled pool $\mathcal{U}$ holds 10,000 held-out images
of the same distribution and never overlaps the labeled sample.
\textbf{Configuration.} One configuration serves every dataset and both
encoders. Truncation keeps $d' = 128$ dimensions, whitening pools the within-group covariance over
$C = K$ $k$-means groups, and smoothing averages each embedding over
$\kappa = 10$ pool neighbors with weight exponent $\gamma = 3$. The
score is the prototype softmax of \eqref{eq:score}. Its temperature $T$
is the one quantity resolved per dataset and encoder, fixed once from a
small pilot split and then held constant across every trial and budget.
Appendix~B reports the values.
\textbf{Baselines.} We compare against split CP on a trained linear
head, scored by the three standard conformal
scores~\citep{sadinle2019least, romano2020aps, angelopoulos2021raps},
SemiCP~\citep{zhou2026semicp}, which augments calibration with
nearest-neighbor-matched unlabeled scores, and
CV+~\citep{barber2021predictive}.
% probe+CP row pending g3 (R2): selftrain / pool-MLP reviewer arms.
Every split-based baseline divides its labels between training and
calibration. We grant each its most favorable allocation per cell,
selected with hindsight from $n_{\mathrm{tr}}/n \in \{0.25, 0.5,
0.75\}$, and its best score, so all comparisons favor the baselines.
SemiCP follows the official implementation (adaptations in Appendix~E).
The unlabeled pool is available to every method that can consume one.
\textbf{Protocol.} The labeled budget is indexed by shots per class
$s = n/K$, tabulated at $s \in \{2, 4, 8, 12\}$. Each cell
averages 50 independent trials that resample the labeled sample, the
test points (5 per class), and where applicable the baseline's
train/calibration split. Labeled samples are class-balanced.
% Random-split arm DROPPED 08-30 (user call). Tables survive at
% output/r1_headline/plots/table_r1b_random_*.tex if it returns.
We report $\alpha = 0.1$ in the main text and $\alpha = 0.05$ in
Appendix~C, with three metrics: marginal coverage, mean prediction-set
size, and
the class-conditional
coverage gap
$\mathrm{CovGap} = \frac{1}{K}\sum_c |\mathrm{cov}_c - (1-\alpha)|$ in
percentage points, reported in Appendix~F.
\subsection{FRCP against trained-head baselines}
\label{sec:headline}
Table~\ref{tab:headline-main} reports mean prediction-set size and
marginal coverage across the budget grid, and
Table~\ref{tab:runtime-cifar100} the compute cost of every method.
Appendix~\ref{app:backbones} repeats the comparison on the CLIP ViT-L
encoder.
\begin{table}[t]
\caption{Mean prediction-set size ($\pm$ standard error) with marginal
coverage in parentheses, on DINOv2 ViT-B at target miscoverage
$\alpha = 0.1$ over 50 trials. Split CP and SemiCP take their best
score and train fraction in each cell, so every comparison favors the
baselines. Bold marks the smallest set per dataset and budget.}
\label{tab:headline-main}
\begin{center}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}
\toprule
dataset & method & 2 shots & 4 shots & 8 shots & 12 shots\\
\midrule
\multirow{4}{*}{CIFAR-10} & Split CP (best) & 9.81$\pm$0.04 {\scriptsize(0.988)} & 2.61$\pm$0.25 {\scriptsize(0.944)} & 1.06$\pm$0.04 {\scriptsize(0.921)} & 0.96$\pm$0.01 {\scriptsize(0.916)}\\
 & CV+ & 3.59$\pm$0.27 {\scriptsize(0.951)} & 1.17$\pm$0.04 {\scriptsize(0.922)} & 0.95$\pm$0.01 {\scriptsize(0.914)} & 0.93$\pm$0.01 {\scriptsize(0.916)}\\
 & SemiCP (best) & 6.36$\pm$0.46 {\scriptsize(0.930)} & 1.61$\pm$0.16 {\scriptsize(0.892)} & 1.01$\pm$0.04 {\scriptsize(0.897)} & 0.93$\pm$0.01 {\scriptsize(0.904)}\\
 & FRCP (ours) & \textbf{0.99$\pm$0.01 {\scriptsize(0.945)}} & \textbf{0.93$\pm$0.01 {\scriptsize(0.921)}} & \textbf{0.92$\pm$0.01 {\scriptsize(0.910)}} & \textbf{0.91$\pm$0.01 {\scriptsize(0.908)}}\\
\midrule
\multirow{4}{*}{CIFAR-100} & Split CP (best) & 92.92$\pm$0.15 {\scriptsize(0.931)} & 8.76$\pm$1.09 {\scriptsize(0.945)} & 1.93$\pm$0.05 {\scriptsize(0.918)} & 1.49$\pm$0.02 {\scriptsize(0.913)}\\
 & CV+ & 8.34$\pm$0.50 {\scriptsize(0.952)} & 2.09$\pm$0.03 {\scriptsize(0.914)} & 1.49$\pm$0.02 {\scriptsize(0.911)} & 1.32$\pm$0.01 {\scriptsize(0.910)}\\
 & SemiCP (best) & 91.94$\pm$0.23 {\scriptsize(0.920)} & 5.83$\pm$0.76 {\scriptsize(0.927)} & 1.78$\pm$0.04 {\scriptsize(0.909)} & 1.44$\pm$0.02 {\scriptsize(0.909)}\\
 & FRCP (ours) & \textbf{2.43$\pm$0.09 {\scriptsize(0.939)}} & \textbf{1.41$\pm$0.02 {\scriptsize(0.908)}} & \textbf{1.26$\pm$0.01 {\scriptsize(0.901)}} & \textbf{1.22$\pm$0.01 {\scriptsize(0.899)}}\\
\midrule
\multirow{4}{*}{EuroSAT} & Split CP (best) & 9.75$\pm$0.06 {\scriptsize(0.980)} & 3.97$\pm$0.27 {\scriptsize(0.944)} & 2.04$\pm$0.13 {\scriptsize(0.915)} & 1.58$\pm$0.07 {\scriptsize(0.918)}\\
 & CV+ & 4.76$\pm$0.23 {\scriptsize(0.948)} & 2.19$\pm$0.08 {\scriptsize(0.902)} & 1.52$\pm$0.03 {\scriptsize(0.916)} & \textbf{1.31$\pm$0.02 {\scriptsize(0.913)}}\\
 & SemiCP (best) & 7.47$\pm$0.38 {\scriptsize(0.946)} & 3.80$\pm$0.20 {\scriptsize(0.908)} & 1.65$\pm$0.10 {\scriptsize(0.884)} & 1.44$\pm$0.06 {\scriptsize(0.903)}\\
 & FRCP (ours) & \textbf{3.59$\pm$0.17 {\scriptsize(0.941)}} & \textbf{2.06$\pm$0.08 {\scriptsize(0.916)}} & \textbf{1.51$\pm$0.04 {\scriptsize(0.901)}} & 1.44$\pm$0.03 {\scriptsize(0.907)}\\
\bottomrule
\end{tabular}
\end{center}
\end{table}
\textbf{Below the boundary, spending every label on calibration wins.}
At $s = 2$ split CP and SemiCP return 64 to 98 percent of the $K$
candidates on every dataset, while FRCP returns 1.0 to 3.6 labels and
the smallest set in all three cells. CV+ is the only competitive
alternative there, at 1.3 to 3.6 times the FRCP size and under the
weaker $1 - 2\alpha$ guarantee. Across the table FRCP is smallest in
11 of 12 cells.
\textbf{Set sizes are comparable only because coverage is.} FRCP holds
marginal coverage between 0.899 and 0.945 over the whole grid, never
more than 0.001 below the target. SemiCP falls below it in three
cells, reaching 0.884 on EuroSAT, so part of the size advantage it
shows at larger budgets is bought back from coverage rather than
earned on efficiency.
\textbf{The advantage narrows as the budget grows.} FRCP is still
smallest on every dataset at $s = 8$. By $s = 12$ CV+ passes it on
EuroSAT, 1.31 against 1.44, and the margins on CIFAR-10 and CIFAR-100
are down to a few percent. Neither shift reaches the small-budget
ranking, where CV+ remains the strongest baseline in every cell. The
picture repeats on the larger CLIP ViT-L encoder
(Appendix~\ref{app:backbones}), where FRCP is smallest in 7 of 9 cells
at $\alpha = 0.1$ and 8 of 9 at $\alpha = 0.05$, again with EuroSAT
the only exception.
\begin{table}[t]
\caption{Per-trial compute time in seconds on CIFAR-100 ($K = 100$,
500 test points, single GPU, median over 5 trials), timed standalone
on the headline protocol. Split-based rows include their probe fit and
all three scores, and CV+ its per-fold probe fits. FRCP additionally
pays a one-off pool-transform fit of 13.4\,s and a 0.3\,s temperature
pilot, shared by every trial, budget and test batch.}
\label{tab:runtime-cifar100}
\begin{center}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{lcccc}
\toprule
method & 2 shots & 4 shots & 8 shots & 12 shots\\
\midrule
Split CP & 0.40 & 0.70 & 0.80 & 0.95\\
CV+ & 0.34 & 1.76 & 2.94 & 3.75\\
SemiCP & 0.76 & 1.15 & 1.40 & 1.73\\
FRCP (ours) & 0.78 & 0.83 & 0.99 & 1.32\\
\bottomrule
\end{tabular}
\end{center}
\end{table}
\textbf{Runtime.} Table~\ref{tab:runtime-cifar100} answers the
tractability question directly. FRCP conformalizes the 500-point
CIFAR-100 test set in 0.8 to 1.3 seconds per trial across the whole
grid, after the one-off pool-transform fit that every trial, budget
and test batch shares. CV+ starts cheapest and ends most expensive, at
3.75 seconds by $s = 12$, almost three times the FRCP cost: its
per-fold probes are refit as the budget grows, while the closed-form
update of \eqref{eq:score} leaves FRCP nearly flat. Split CP stays the
cheapest construction throughout and pays for it in set size at the
small budgets above. On the two $K = 10$ datasets every method
finishes in under half a second and CV+ is again the most expensive by
$s = 12$, 0.41 against 0.09 seconds for FRCP on EuroSAT. The one
baseline that competes with FRCP on set size at small budgets is also
the one whose cost grows fastest with the budget.
\textbf{Full CP or the score?} Every baseline differs from FRCP in both
the construction and the score, so Appendix~\ref{app:attrib} varies only
the construction. Jackknife+ with the same score reproduces FRCP set for
set, so full CP earns the exact guarantee rather than efficiency. Split
CP with the same score loses at two shots and converges by eight without
collapsing, which places most of the margin on the training-free score
and the rest on spending every label on calibration.
\subsection{Refinement ablation}
\label{sec:ablation}
Figure~\ref{fig:ablation} ablates the refinement stage by stage on
every dataset under the deployed score: the raw embedding, each single
stage, the truncation-plus-whitening pair, and the full chain.
Appendix~D tabulates the decomposition, and
Appendix~\ref{app:scorehist} shows what the surviving subset does to
the score distributions.
\begin{figure}[t]
\begin{center}
\includegraphics[width=\linewidth]{fig_stage_ablation_row.pdf}
\end{center}
\caption{Stage ablation. Mean prediction-set size for the raw
embedding, each single refinement stage, the T$+$W pair, and the full
chain, with truncation (T), whitening (W), and smoothing (S), at 2, 4,
and 8 labels per class and $\alpha = 0.1$ over 20 trials. The dot
matrix marks the active stages. Bars above a panel cap are clipped and
annotated with their value.}
\label{fig:ablation}
\end{figure}
\textbf{Refinement pays most at small budgets, and only the full chain
delivers it.} The full pipeline returns the smallest sets in all nine
dataset and budget cells of Figure~\ref{fig:ablation}. On CIFAR-100 it
reduces set size by 79\% at $s = 2$ (10.7 $\to$ 2.27), by 36\% at
$s = 4$, and by 11\% at $s = 8$. No single stage gets below 4.16 there
at two shots. Truncation and whitening together deliver the largest
step, and smoothing removes up to a further third (3.35 $\to$ 2.27).

\textbf{Order matters, not just membership.} Whitening alone
destabilizes the temperature fit, which collapses to its floor on every
dataset, and its sets inflate past the raw embedding on EuroSAT at all
three budgets (6.40 against 5.51 at $s = 2$). With truncation first the
fit is stable and the pair improves on either stage alone on CIFAR-10
and CIFAR-100. EuroSAT only breaks even at the pair, and there
smoothing lands the decisive step (4.32 $\to$ 3.65 at $s = 2$), which
is the ordering Section~\ref{sec:refine} fixes.

% --- PARKED 2026-08-31 (user call): the score-family presentation is
% OPEN, both whether to address it and how. fig_r1b_ncm_* and the
% Appendix D tables still carry the material. Original paragraph:
% \textbf{Score family.} The softmax normalization tightens the cosine
% score by 18--26\% across budgets, and the top-$k$ ratio trails both
% on this separable dataset.
\textbf{Refinement separates the score mass around the quantile.}
Appendix~\ref{app:scorehist} plots the true-class and false-class
distributions of the deployed prototype-softmax score before and after
refinement. Refinement concentrates the true-class mass below the
calibration quantile and pushes false-class mass above it, so the same
coverage is bought with fewer false labels in the set: at four labels
per class the leak falls from 0.55 to 0.02 on CIFAR-10 and from 1.50
to 0.42 on CIFAR-100. Even on EuroSAT, where the two masses stay
entangled, the leak drops from 1.84 to 1.56.

\textbf{Smoothing the representation, not the score.}
Appendix~\ref{app:snaps} compares the two levels at which a pool
correction can act (Table~\ref{tab:snaps-vs-smoothing}): the
representation-level smoothing stage recovers more of the refinement
gain than a score-level SNAPS correction at the small budgets that
matter, at comparable per-trial cost, and stacking the two adds little.


% --- provenance comments -----------------------------------------------
% Temperature values must be re-verified against the results JSONs
% before they are written into Appendix B (v4 note still open). The
% wording "fixed once from a small pilot split" is deliberate: T is
% fitted on a labeled pilot split (resolve_softmax_T), not from pool
% statistics alone.
% --- appendix stock ---------------------------------------------------
% Stage table (r1b decomposition, 50 trials, raw / +T,W / full):
% output/r1_headline/plots/table_r1b_cifar100.tex. The 2^3 grid behind
% Figure 3 is tabulated in
% output/pipeline_ablation/plots/stage_ablation_stats.json (20 trials,
% six combos post 09-06 rerun on the three-dataset roster);
% generate a booktabs table from it if Appendix D wants the full grid.
